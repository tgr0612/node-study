crypto 모듈은 해시 생성 및 암호화와 관련된 모듈입니다.

body-parser 미들웨어는 POST로 요청했을 때 요청 파리마터를 확인할 수 있습니다.

쿠키는 웹 브라우져 안에 저장 / 세션은 웹서버에 저장되는 정보입니다.

multer 미들웨어는 파일 업로드를 위한 것입니다.

스키마란 ? 데이터베이스의 구조를 정의한 것을 말합니다.

mongoose에서 virtual()함수를 사용하면 비밀번호 암호화를 할 수 있다.
